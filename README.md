# GPU and CPU computations

Labs from the GPU computing course, each in its own directory with the code and the
measurement scripts.

| Lab | Topic | Measured on |
|-----|-------|-------------|
| [CPU and memory subsystem](cpu) | How the cache hierarchy, stride, element size, TLB and branch prediction show up in the hardware counters of a DAXPY loop | Apple M3 Pro, PMU through `kperf` |
